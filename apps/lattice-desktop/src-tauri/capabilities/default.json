{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Lattice main window. Grants the baseline core permissions needed for the Leptos WebView to render plus access to the lattice-media IPC commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
